---
export interface Props {
  highlights: string[];
  title?: string;
}

const { highlights, title = "Punti di Interesse" } = Astro.props;
---

<div>
  <h3 class="text-2xl font-bold text-trail-green mb-4">{title}</h3>
  <ul class="space-y-3">
    {highlights.map(highlight => (
      <li class="flex items-start">
        <svg class="w-5 h-5 text-trail-gold mt-1 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
        <span class="text-gray-700">{highlight}</span>
      </li>
    ))}
  </ul>
</div>
